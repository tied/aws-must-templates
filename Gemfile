# -*- mode: ruby -*-

source 'https://rubygems.org'
gem 'aws-must', "~>0.0.12.pre"

# System tests
gem 'serverspec'

# Unit tests
gem 'rspec'

# Guard
gem 'guard'
gem 'guard-rspec'
gem 'guard-shell'
gem 'rb-readline'
